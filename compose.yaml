services:
  setup-service:
    build:
      context: .
    env_file:
      - .env.service
    ports:
      - "127.0.0.1:8787:8787"
    read_only: true
    init: true
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
